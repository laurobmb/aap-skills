#!/usr/bin/env bash
# Atalho na raiz do repo → scripts da skill aap-operate
set -euo pipefail
SKILL_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.cursor/skills/aap-operate" && pwd)"
script="${1:?Uso: ./operate <script-name.sh> [args...]}"
shift
exec "${SKILL_ROOT}/scripts/${script}" "$@"
